# 映射 L 为下一个标签页 (Next)，H 为上一个 (Previous)
# 这样符合 L 在右、H 在左的直觉
map L nextTab
map H previousTab

# 使用 unmap (注意不是 umap) 取消 J 和 K 的默认映射
unmap J
unmap K

# 既然 J 和 K 空出来了，建议把它们映射为垂直滚动 (比默认的 j/k 更快)
map J scrollPageDown
map K scrollPageUp